package com.omnirank.repository;
import com.omnirank.model.PlatformHandle;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlatformHandleRepository extends JpaRepository<PlatformHandle, Long> {}